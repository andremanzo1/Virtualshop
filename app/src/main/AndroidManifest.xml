<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Virtualshop"
        tools:targetApi="31">

        <activity
            android:name=".LandingPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Add any intent filters or other configuration specific to MainActivity -->
        </activity>

        <activity
            android:name=".LoginActivity">
            <!-- Add any intent filters or other configuration specific to LoginActivity -->
        </activity>

        <activity
            android:name=".WelcomeHomeActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".AdminLoginActivity"
            android:exported="true">
        </activity>

        <!-- Add the registration for AdminHomeActivity here -->
        <activity
            android:name=".AdminHomeActivity"
            android:exported="true">
            <!-- Add any intent filters or other configuration specific to AdminHomeActivity -->
        </activity>

    </application>

</manifest>